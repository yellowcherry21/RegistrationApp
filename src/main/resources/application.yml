server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    username: postgres
    password: admin
    url: jdbc:postgresql://localhost:5432/registration
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
  mail:
    host: localhost
    port: 1025
    username: vasia
    password: sa

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true